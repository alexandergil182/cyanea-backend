info:
  name: Listar Producto
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url_local}}/v1/api/productos/"
  params:
    - name: activo
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
