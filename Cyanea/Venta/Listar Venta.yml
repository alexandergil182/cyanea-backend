info:
  name: Listar Venta
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url_local}}/v1/api/ventas/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
